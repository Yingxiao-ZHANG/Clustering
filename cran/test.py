from cran.patent import cut_spectral_density


if __name__ == "__main__":
    cut_spectral_density()
    # Plot the clustering topology of massive MIMO
    # topology_spectral_max_beam()

    # Plot the clustering topology of slow-scale spectral clustering and fast update
    # topology_spectral_fast()

    # Plot the topology of spectral clustering
    # topology_spectral()

    # User-based clustering
    # cluster_usr()
